<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@10"></script>
    <script src="https://www.google.com/recaptcha/api.js" async defer></script>
    <link rel="manifest" href="/manifest.json" />
    <style>
        body {
            background-color: #f8f9fa;
        }

        .footer {
            position: absolute;
            bottom: 0;
            width: 100%;
            height: 60px;
            background-color: #f5f5f5;
          }
        
        .card {
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
            max-width: 400px;
            margin: 0 auto;
            border-radius: 15px;
            border: none;
            overflow: hidden;
        }
        .card-header {
            background-color: #FCF5ED;
            color: #fff;
            text-align: center;
            padding: 20px 0;
            border-bottom: none;
        }
        .card-body {
            padding: 30px;
        }
        .form-control {
            height: 50px;
            font-size: 16px;
        }
        .form-label {
            font-weight: bold;
        }
        .btn-login {
            transition: all 0.3s ease;
        }
        .btn-login:hover {
            transform: scale(1.05);
        }
        .invalid-feedback {
            color: #dc3545;
            font-size: 14px;
        }
    </style>
</head>
<body>
    <div class="container mt-5">
        <div class="card">
            <div class="card-header">
                <img src="https://imgdb.net/storage/uploads/a461f4ba9c9f1f40f9d98c069a8ea5d4b089acf945e0bbeadcd3e13fbafdc21f.png" alt="Logo" width="100" />
            </div>
            <div class="card-body">
                <h3 class="text-center mb-4">Login</h3>
                <form action="/login" method="post">
                    <div class="mb-3">
                        <label for="username" class="form-label">Username</label>
                        <input type="text" class="form-control" id="username" name="username" required>
                        <div class="invalid-feedback">Username is required.</div>
                    </div>
                    <div class="mb-3">
                        <label for="password" class="form-label">Password</label>
                        <input type="password" class="form-control" id="password" name="password" required>
                        <div class="invalid-feedback">Password is required.</div>
                    <small>Belum memiliki username dan password? <a href="https://wa.me/6285255646434">Klik Di Sini</a></small>
                    </div>
                    <div class="d-grid gap-2">
                        <button type="submit" class="btn btn-primary btn-login">Login</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
    <script>
        function showSuccessAlert() {
            Swal.fire({
                icon: "success",
                title: "Success",
                text: "Login successful!",
            });
        }

        function showErrorAlert() {
            Swal.fire({
                icon: "error",
                title: "Oops...",
                text: "Username or password is incorrect!",
            });
        }

        let urlParams = new URLSearchParams(window.location.search);
        let loginStatus = urlParams.get("status");
        if (loginStatus === "success") {
            showSuccessAlert();
        } else if (loginStatus === "error") {
            showErrorAlert();
        } 
    </script>
    <footer class="footer">
      <div class="container">
        <span class="text-muted">
            <%= appName %> - <%= appVersion %>
        </span>
        <br />
        <small>Made with ❤️ by <%= autor %></small>
      </div>
    </footer>
</body>
</html>
